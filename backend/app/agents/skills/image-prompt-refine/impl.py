"""image-prompt-refine skill — see SKILL.md. Lands in GH-11."""

from app.agents.tools import gemini_text


async def run(image_prompt: str, image_style_directives: str) -> str:
    raise NotImplementedError("Lands in GH-11 with prompt from prompts/image_prompt_refine.md.")
